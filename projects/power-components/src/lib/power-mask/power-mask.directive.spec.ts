import { PowerMaskDirective } from './power-mask.directive';

describe('PowerMaskDirective', () => {
  it('should create an instance', () => {
    const directive = new PowerMaskDirective();
    expect(directive).toBeTruthy();
  });
});
